def file = new File('data.sql')

task handleFile{
    onlyIf(new Spec(){
        boolean isSatisfiedBy(task){
            println '2'+file.exists()
            file.exists()
        }
    })


    doLast {
        println "Work with file ${file.name}"
    }
}
